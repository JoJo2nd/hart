
include "textureformats.fbs";

namespace hart.render.resource;

table Texture {
    // If a texture needs custom formatting, add enum to catch that here.
    data:[ubyte];
}

file_identifier "texr";
root_type Texture;